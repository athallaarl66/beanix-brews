# ===============================
# DATABASE (PostgreSQL - Neon)
# ===============================

# 🔹 Dipakai aplikasi (Next.js runtime / API routes)
# 🔹 WAJIB pakai connection pooling
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# 🔹 Dipakai Prisma CLI (migrate, seed, reset)
# 🔹 TANPA pooling (direct connection)
DATABASE_URL_DIRECT="postgresql://user:pass@host/db?sslmode=require"
